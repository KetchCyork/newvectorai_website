# Use npm run admin:setup to generate secrets in .local/cloudflare-secrets.json.
# Production: npx wrangler deploy --secrets-file .local/cloudflare-secrets.json
# Keep secret values out of this file and version control.
ADMIN_PASSWORD_HASH=
ADMIN_PASSWORD_SALT=
SESSION_SECRET=
SETTINGS_KEY=
